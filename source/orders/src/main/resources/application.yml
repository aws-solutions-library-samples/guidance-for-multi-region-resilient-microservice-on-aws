management:
  endpoints:
    web:
      exposure:
        include: '*'

server:
  port: ${port:8080}

logging:
  level:
    root: debug

spring:
  jpa:
    generate-ddl: true
    show-sql: true
    hibernate:
      ddl-auto: update
  autoconfigure.exclude:
  - org.springframework.boot.autoconfigure.jms.activemq.ActiveMQAutoConfiguration
